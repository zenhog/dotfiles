monitor.alsa.rules = [
  {
    matches = [
      {
        node.description = "Built-in Audio Analog Stereo"
      }
    ]
    actions = {
      update-props = {
        node.description = "System"
        priority.driver = 100,
        priority.session = 100,
      }
    }
  }
]
