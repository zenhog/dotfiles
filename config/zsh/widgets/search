search() {
  local indir="$(tmux set -s @menu-search-altdir "$(pwd)" 2>/dev/null)"
  menu popup search list

  local dir="$(tmux show -sv @menu-search-cdir 2>/dev/null)"

  [[ -n "$dir" ]] && cd "$dir" && zle accept-line
}
