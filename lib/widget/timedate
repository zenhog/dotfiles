#!/usr/bin/env bash

PERIOD=1

text() {
	local time=$(date '+%R')
	local time=$(date '+%I:%M %p')
	local time=$(date '+%H:%M %a %b %d')

	printf "%s" "$time"
}

icon() {
	local icon=󱑍
	printf "%s" "$icon"
}

output() {
	printf ":"
	text
}
