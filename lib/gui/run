#!/usr/bin/env bash

KEY=r
MOD='S-M'
ICON=

export APP_DIR="$HOME/.lib/app"

function view() {
  command preview "$APP_DIR/$1"
}

function list() {
  local apps
  readarray -t apps < <(ls -1 $HOME/.lib/app)
  printf "%s\n" "${apps[@]}"
}

function key() {
  source "$HOME/.lib/app/$1"
  echo "$1:$KEY"
}

function keys() {
  for app in $(basename -a "${APP_DIR}"/*); do
    gui run key "$app"
  done
}

BINDS[edit]=alt-e
HOOKS[edit]=accept
DESCS[edit]='Edit in $EDITOR'
function edit() {
  open "$APP_DIR/$1"
}

function irun() {
  source "$HOME/.lib/app/$1"
  if silent declare -F runner; then
    runner
  fi
}

BINDS[fg]=enter
HOOKS[fg]=accept
DESCS[fg]='Run in the foreground'
function fg() {
  source "$HOME/.lib/app/$1"
  if silent declare -F runner; then
    gui awm runonce fg "${ATTRIBUTES[@]}" -- gui run irun "$1"
  else
    gui awm runonce fg "${ATTRIBUTES[@]}" -- "${COMMAND[@]}"
  fi
}

BINDS[bg]=tab
DESCS[bg]='Run in the background'
function bg() {
  source "$HOME/.lib/app/$1"
  gui awm runonce bg "${ATTRIBUTES[@]}" -- "${COMMAND[@]}"
}

BINDS[close]=alt-d
DESCS[close]='Terminate window'
function close() {
  source "$HOME/.lib/app/$1"
  gui awm winkill "${ATTRIBUTES[@]}"
}
